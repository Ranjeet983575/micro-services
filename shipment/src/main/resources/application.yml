server:
  port: 8094

spring:
  application:
    name: ms_shipment
  datasource:
    url: jdbc:postgresql://localhost:5432/ms_shipment
    username: postgres
    password: Ranjeet@123#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
